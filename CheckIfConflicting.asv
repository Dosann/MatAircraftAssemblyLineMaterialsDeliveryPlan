function feasib=CheckIfConflicting(place_pos,arriv_time)
global Case Paras

if sum(place_pos(4,:))>0
    feasib=0;
    return;
end

% 物料中心摆放位置lms
lms=ceil(Case.position'+((Case.a+Case.b)/2)'*Paras.v);

area=zeros(Paras.D,Paras.L,Paras.C);

for jobid=1:Case.N
    area_to_place=sum(sum(area(arriv_time(jobid):Case.b(jobid),lms(jobid)